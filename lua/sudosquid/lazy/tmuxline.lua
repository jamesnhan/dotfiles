-- Only uncomment this to initially install the plugin. Then, disable it to
-- speed up Neovim startup time or else it will take forever to launch.
return {
    -- "edkolev/tmuxline.vim",
    -- config = function()
    --     vim.cmd.Tmuxline()
    --     vim.cmd.TmuxlineSnapshot("$HOME/.tmux/tmuxline.vim!")
    -- end,
}

